export class Animal {
  public done: boolean = true;
  constructor(public species: string, public name: string, public age: number, public diet: string, public zooLocation: string, public needCare: number, public sex: string, public oneLike: string, public oneDislike: string){ }
}
